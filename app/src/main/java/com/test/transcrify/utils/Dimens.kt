package com.test.transcrify.utils // O dove preferisci

import androidx.compose.ui.unit.dp

object Dimens {
    // Dimensioni FloatingBase (Main)
    val FloatingBaseWidth = 20.dp
    val FloatingBaseHeight = 100.dp
    val FloatingBaseCornerRadius = 20.dp // Metà larghezza per arrotondare

    // Dimensioni ExpandedMenu (Expanded)
    val ExpandedMenuWidth = 70.dp
    val ExpandedMenuHeight = 280.dp
    val ExpandedMenuCornerRadius = 35.dp // Metà larghezza per arrotondare

    // Margine Base dal bordo schermo
    val FloatingBaseMarginEnd = 0.dp

    // Altre dimensioni che potresti voler centralizzare...
    val CircularButtonSize = 56.dp
    val CircularButtonIconSize = 30.dp
    val SmallIconButtonSize = 24.dp
}